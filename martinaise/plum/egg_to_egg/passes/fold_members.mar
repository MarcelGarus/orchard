import mod.mar

fun fold_members(id: EggId, builder: &EggBodyBuilder): EggId {
  var member = id.resolve().member or return id

  if member.of.resolve() is struct_(fields) then
    return fields.get(member.name)

  id
}
