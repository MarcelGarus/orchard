import mod.mar

fun simplify_boxes(id: EggId, builder: &EggBodyBuilder): EggId {
  switch id.resolve()
  case unbox(box) {
    if box.resolve() is box(inner) then return inner
  }
  default {}

  id
}
