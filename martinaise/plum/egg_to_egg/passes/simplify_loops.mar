import mod.mar

fun simplify_loops(id: EggId, builder: &EggBodyBuilder): EggId {
  var loop_ = id.resolve().loop_ or return id

  if not(loop_.body.contains_continue()) then
    return loop_.body.fill(loop_.initial, builder)
  
  id
}
