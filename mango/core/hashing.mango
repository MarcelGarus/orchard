## Hashing  
# You should implement a "hash(T): Hash" function for types that can be hashed.
# In the end, all hash functions boil down hashing integers.

import stdlib.mar

struct Hash { value: Int }

fun hash(value: Int): Hash { Hash { value } }
fun to_int(hash: Hash): Int { hash.value }
fun combine(a: Hash, b: Hash): Hash { a * 33 + b }

# TODO
# fun hash_all[I](hasher: &Hasher, iter: I) {
#   for item in iter do hasher.hash(item)
# }
