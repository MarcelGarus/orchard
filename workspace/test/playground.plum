import
  "...core"
    Array t, generate_array Int (\ Int -> t), len (Array t), Int, foo Int,
    slice (Array t) (Range Int), Range t, to t t,
    Byte, lower_byte Int, to_int Byte, format Int, push (List t) t, list t t t,
    List t, String, list t, set (Array t) Int t

main a: Int -> String =
  list 1 2 3 .push 2 .push 3 .push 4. push 5 .push 6 .push 7 .push 8 .push 9
  "Hi"
